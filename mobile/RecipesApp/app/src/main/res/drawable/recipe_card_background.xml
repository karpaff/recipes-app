<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Внешний слой для обводки -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/pantone" />
            <corners android:radius="10dp" />
        </shape>
    </item>

    <!-- Внутренний слой для контента -->
    <item android:left="1.5dp" android:top="1.5dp" android:right="1.5dp" android:bottom="1.5dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="8dp" /> <!-- Радиус немного меньше для "вложенности" -->
        </shape>
    </item>
</layer-list>
